name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Replace Secrets
        env:
          # Try all common variations of the name
          OPENAI_KEY_1: ${{ secrets.OPENAI_API_1 }}
          OPENAI_KEY_2: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_KEY_3: ${{ secrets.OPENAI_KEY }}
          OPENAI_KEY_4: ${{ secrets.OPENAI }}
          DEEPGRAM_KEY_1: ${{ secrets.DEEPGRAM_API }}
          DEEPGRAM_KEY_2: ${{ secrets.DEEPGRAM_API_KEY }}
          DEEPGRAM_KEY_3: ${{ secrets.DEEPGRAM_KEY }}
        run: python .github/scripts/replace_secrets.py

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./
          force_orphan: true
